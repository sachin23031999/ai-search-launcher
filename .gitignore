# --- Kotlin / Gradle (app/) ---
.gradle/
**/build/
!src/**/build/
*.hprof

# --- .NET (mcp-server/) ---
[Bb]in/
[Oo]bj/

# --- Assembled MCP server bundle (generated by :app:bundleMcpServer) ---
app/resources/

# --- Vendored toolchain (self-contained JDK / Gradle / .NET; not source) ---
# Anchored to repo root so it does not also match mcp-server/Tools/ (git is
# case-insensitive on Windows, and an unanchored "tools/" matches any depth).
/tools/

# --- IDE / OS ---
.idea/
*.iml
.DS_Store
Thumbs.db

# --- Local secrets ---
*.local
.env

# --- Machine-specific Gradle config ---
local.properties
